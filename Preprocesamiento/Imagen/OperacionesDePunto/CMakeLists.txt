cmake_minimum_required(VERSION 2.8) 
project(OperacionesDePunto)
find_package(ITK REQUIRED)
include(${ITK_USE_FILE})
add_executable(OperacionesDePunto MACOSX_BUNDLE Operaciones.cxx)
target_link_libraries(OperacionesDePunto ${ITK_LIBRARIES})
